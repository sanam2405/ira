[project]
name = "api"
version = "0.1.0"
author = "Manas Pratim Biswas <manaspb2405@gmail.com>"
description = "ira — FastAPI search service"
requires-python = ">=3.12"
dependencies = [
    "core",
    "fastapi[standard]>=0.115.12",
]

[tool.uv.sources]
core = { workspace = true }
