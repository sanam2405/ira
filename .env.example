# Copy to .env and fill in. Settings map to fields in core/core/config.py.
GEMINI_API_KEY=

# Optional overrides:
# EMBEDDING_MODEL=gemini-embedding-001
# EMBEDDING_DIMENSIONS=768
# TRANSLATION_MODEL=gemini-2.5-flash

# Throughput:
# EMBEDDING_CONCURRENCY=8
# TRANSLATION_CONCURRENCY=8
# USE_BATCH_API=false        # true -> Gemini Batch API for embeddings (50% cheaper, async)
